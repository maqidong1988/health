define(function(require, exports, module) {
	module.exports = {
		hasPermission: function(permission) {
			var permissionStr = "|" + permission + "|";
			return $$permissions.indexOf(permissionStr) > 0; // $$permissions see also: index.jsp 18th Line
		}
	};
});